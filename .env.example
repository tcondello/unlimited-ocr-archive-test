# Copy this file to .env and fill in your values:
# cp .env.example .env

# Required to download baidu/Unlimited-OCR from the Hugging Face Hub
HF_TOKEN=

# Optional — only needed if you want to re-run the Claude vision baseline
# in the same script. The committed outputs/ already include Claude results.
ANTHROPIC_API_KEY=
